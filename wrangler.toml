name = "rss-pipeline-api"
main = "src/index.ts"
compatibility_date = "2026-06-27"
compatibility_flags = ["nodejs_compat"]

[[queues.producers]]
binding = "QUEUE"
queue = "article-extraction-queue"

[vars]
# 敏感凭据（SUPABASE_* / FEISHU_*）通过 wrangler secret put 注入，不在此声明
# 以下为非敏感公开配置
SITE_TITLE = "RSS Pipeline"
SITE_URL = "https://rss-pipeline-api.wx-yyyz-ai.workers.dev"