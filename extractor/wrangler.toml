name = "rss-pipeline-extractor"
main = "src/extractor.ts"
compatibility_date = "2026-06-27"
compatibility_flags = ["nodejs_compat"]

[[queues.consumers]]
queue = "article-extraction-queue"
max_batch_size = 10
max_batch_timeout = 30

# 敏感凭据通过 wrangler secret put 注入，不在此声明
# [vars] 暂无非敏感配置